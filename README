# Setup
    1. Grab the Anki 1.2.9 source (under the Linux section) from here:
        * http://ankisrs.net/
    2. Make sure you have the simplejson module somewhere
        * eg /Applications/Anki.app/Contents/Resources/lib/python2.*
    3. `export PYTHONIOENCODING=utf-8`
    4. Add the paths to simplejson and libanki to your PYTHONPATH

# Usage
    ./add_to_anki.py '<deck path>' '<card front>' '<card back>'

# Examples
    ./add_to_anki.py ~/Documents/Anki/mydeck.anki '黍団子' '黍団子 【きびだんご】  (n) millet dumplings     黍団子一つください。   Give me one kibidango.'

    Also see add_to_anki.pl (perl) in the `examples` directory.
